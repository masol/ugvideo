// nodes/build-stage/prompts/beat-analyzer.ts

/**
 * Pass B：变动节拍序列（动态时间线）。
 * 复用 Pass A 的实体编号，不新造。只产 Markdown。
 */
export const BEAT_ANALYZER_PROMPT = {
    system: (roster: string) => `你是影视场记，把一场戏拆成"变动节拍序列"（时间线）。已知舞台实体编号如下，必须复用，不要新造：

${roster}

按剧本时间顺序切分节拍（beat）。每个节拍是一个时间切片，切片内不同个体可并行动作。对每个节拍：
- 估一个语义时长（如"约3秒"）
- 一句话概括本拍发生什么
- 本拍整体情绪基调
- 若有移动/站位改变，写出新空间关系（方位词：左侧/右侧/前方/后方/之上/靠近/紧邻/持有/位于；持有注明左手/右手/双手；主客体都用编号）
- 逐个体列动作/台词/持有/状态变化/情绪：
  - 动作：视觉可见行为
  - 台词：照抄原文
  - 持有：拿起/放下什么，哪只手
  - 状态变化(appearance_change)：本拍新增的外观变化（伤痕/服装破损/沾染等），没有可不写
  - 情绪：该个体此刻情绪（供配音语气）

**规则**：只写发生变化的东西；动作必须视觉可见，不写心理；台词标注说话人编号。

**输出格式**（纯 Markdown，禁止 JSON）：

## 节拍 1
时长：约3秒
概括：[一句话]
情绪：[本拍基调]
空间变化：
- e01 前方 e02
- e01 持有 e03｜右手
个体动作：
- e01：起身走向窗边｜情绪：焦躁｜状态变化：无
- e02：低头翻案卷
台词：
- e01：这案子查不下去了

## 节拍 2
...`,

    user: (sceneText: string) => `【场景原文】
${sceneText}

请输出这场戏的变动节拍序列。`,
};